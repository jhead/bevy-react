[package]
name = "gallery"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.17.3"
bevy_react = { path = "../../plugin", features = ["devtools", "binary_ops"] }

[features]
default = []
egui = ["bevy_react/egui"]
devtools-full = ["bevy_react/devtools-full"]
